[pytest]
# Run suites separately to avoid module name collisions:
#   make test-router
#   make test-operator
# Or from each subdir:
#   cd router && pytest tests/
#   cd operator && pytest tests/
addopts = --import-mode=importlib
